import { colorBorderPaper } from '@/components/theme/dark/dark-theme-values'

export const dashboardStyles = { flex: 1, display: 'grid', gridTemplateColumns: '30% 1fr', gap: '15px', padding: '0 15px 15px' }
export const dashboardItemStyles = { p: 2, borderRadius: 3, border: `1px solid ${colorBorderPaper}`, bgcolor: 'background.paper' }
